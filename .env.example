# Crane AI Agent Runtime - Environment Configuration
# Copy this file to .env and fill in your values

# ==============================================================================
# LLM Configuration
# ==============================================================================

# Option 1: OpenAI API (Production)
# ------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI Model (default: gpt-4o-mini for cost efficiency)
# Alternatives: gpt-4o, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o-mini

# OpenAI Temperature (default: 0.1 for consistent planning)
# Range: 0.0 (deterministic) to 2.0 (creative)
OPENAI_TEMPERATURE=0.1

# Option 2: Local LLM via LiteLLM (Testing/Development)
# -------------------------------------------------------
# Uncomment these to use local LLM instead of OpenAI
# Requires: Ollama + LiteLLM proxy running
# Setup guide: claudedocs/local_llm_testing_guide.md

# OPENAI_BASE_URL=http://localhost:4000
# OPENAI_MODEL=qwen2.5:3b
# OPENAI_API_KEY is automatically set to dummy value when OPENAI_BASE_URL is set
# You can also set it explicitly: OPENAI_API_KEY=sk-any-value-works

# ==============================================================================
# Application Configuration
# ==============================================================================

# Environment (development, staging, production)
ENVIRONMENT=development

# API Server Configuration
HOST=0.0.0.0
PORT=8000

# Debug Mode (enables auto-reload and detailed error messages)
DEBUG=true

# Log Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ==============================================================================
# Orchestrator Configuration
# ==============================================================================

# Maximum retry attempts for failed steps
MAX_RETRIES=3

# Timeout for each step execution (seconds)
STEP_TIMEOUT=30.0

# ==============================================================================
# CORS Configuration (for frontend integration)
# ==============================================================================

# Comma-separated list of allowed origins
# CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# ==============================================================================
# Security (Optional - for production)
# ==============================================================================

# API Key for authentication (if implementing auth)
# API_KEY=your-secret-api-key

# ==============================================================================
# Notes
# ==============================================================================

# 1. NEVER commit .env to version control (already in .gitignore)
# 2. Keep API keys secure and rotate them regularly
# 3. Use different .env files for different environments
# 4. The application works without OPENAI_API_KEY (uses pattern-based planner)
# 5. For production, set DEBUG=false and use appropriate LOG_LEVEL
